apiVersion: v1
kind: Service
metadata:
  name: fortnite-backend-service
  namespace: backend
spec:
  selector:
    app: fortnite-backend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
